[project]
name = "jubilant"
description = "Juju CLI wrapper, primarily for charm integration testing"
readme = "README.md"
requires-python = ">=3.10"
dynamic = ["version"]
authors = [
    {name="The Charm Tech team at Canonical Ltd."},
]
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: Apache Software License",
    "Development Status :: 5 - Production/Stable",
    "Intended Audience :: Developers",
    "Intended Audience :: System Administrators",
    "Operating System :: MacOS :: MacOS X",
    "Operating System :: POSIX :: Linux",
]
dependencies = [
    "PyYAML==6.*",
]

[project.urls]
Homepage = "https://github.com/canonical/jubilant"
Repository = "https://github.com/canonical/jubilant"
Issues = "https://github.com/canonical/jubilant/issues"
Documentation = "https://canonical.com/juju/docs/jubilant/"

[dependency-groups]
dev = [
    "codespell==2.4.3; python_version >= '3.9'",
    "cryptography>=43.0.0",  # for generating SSH keys in tests
    "ops==3.8.0",  # for static type checking test charms
    "pyright==1.1.411",
    "pytest==9.1.1",
    "pytest-cov==7.1.0",
    "ruff==0.16.0",
    "zizmor==1.28.0; python_version >= '3.10'",
]

[build-system]
requires = ["setuptools>=60"]
build-backend = "setuptools.build_meta"

[project.scripts]
jubilant = "jubilant._main:main"

[tool.setuptools.dynamic]
version = {attr = "jubilant.__version__"}

# Workaround for setuptools bug:
# https://github.com/astral-sh/uv/issues/9513#issuecomment-2519527822
[tool.setuptools]
license-files = []

[tool.uv]
# Rolling quarantine: never resolve against packages published in the last 7
# days, so a compromised release has a window to be caught/yanked upstream
# before it can reach this project via a manual `uv add`, a `uv lock` regen,
# a uvx bootstrap, or a CI re-resolve. Complements (doesn't replace) the
# Dependabot cooldown, which only protects Dependabot-authored PRs.
exclude-newer = "7 days"

# Linting tools configuration
[tool.ruff]
line-length = 99
target-version = "py310"
extend-exclude = ["docs/*"]

# Ruff formatter configuration
[tool.ruff.format]
quote-style = "single"
preview = true

[tool.ruff.lint]
select = [
    # Pyflakes
    "F",
    # Pycodestyle
    "E",
    "W",
    # isort
    "I001",
    # pep8-naming
    "N",
    # flake8-builtins
    "A",
    # pyupgrade
    "UP",
    # flake8-2020
    "YTT",
    # flake8-bandit
    "S",
    # flake8-bugbear
    "B",
    # flake8-simplify
    "SIM",
    # Ruff specific
    "RUF",
    # Perflint
    "PERF",
    # pyflakes-docstrings
    "D",
]
ignore = [
    # Function argument `x` is shadowing a Python builtin
    "A002",

    # Use of `assert` detected
    "S101",

    # `subprocess` call: check for execution of untrusted input
    "S603",

    # Return the condition `x` directly
    "SIM103",
]

[tool.ruff.lint.pydocstyle]
convention = "google"

[tool.ruff.lint.flake8-builtins]
builtins-ignorelist = ["id", "min", "map", "range", "type", "input", "format"]

[tool.ruff.lint.per-file-ignores]
"jubilant/modeltypes.py" = [
    # Missing docstring in public class
    "D101",
]
"jubilant/unittypes.py" = [
    # Missing docstring in public class
    "D101",
]
"jubilant/statustypes.py" = [
    # Missing docstring in public class
    "D101",
]
"tests/*" = [
    # All documentation linting.
    "D",
    # Use of possibly insecure function (eval)
    "S307",
    # Line too long
    "E501",
]

[tool.pyright]
include = ["jubilant", "tests"]
pythonVersion = "3.10"
pythonPlatform = "All"
typeCheckingMode = "strict"
reportPrivateUsage = false  # silence errors about use of _from_dict outside class
reportUnnecessaryTypeIgnoreComment = "error"

[tool.pytest.ini_options]
filterwarnings = [
  "error",
]
markers = [
  "machine: integration tests that require a Juju machine model",
]
pythonpath = [
  "."
]
